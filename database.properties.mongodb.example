# Configuración para MongoDB
db.type=mongodb
db.host=localhost
db.port=27017
db.name=taller_db

# Si MongoDB requiere autenticación, descomentar y configurar:
# db.user=tu_usuario
# db.password=tu_contraseña

# Nota: Si NO usas autenticación, deja usuario y contraseña vacíos
db.user=
db.password=

